[project]
name = "router-sql-rag"
version = "0.1.0"
description = "A router over Text-to-SQL and semantic retrieval, in a hexagonal (ports & adapters) layout."
requires-python = ">=3.10"
dependencies = ["fastapi>=0.110", "uvicorn>=0.29"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
